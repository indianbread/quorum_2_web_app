steps:
  - label: "Restore"
    command: "dotnet restore"
    agents:
      dotnet: "true"
      queue: "katherine-lab"
    
  - wait
    
  - label: "Build"
    command: "dotnet build"
    agents:
      dotnet: "true"
      queue: "katherine-lab"
    
  - wait
    
  - label: "Test"
    command: "dotnet test"
    agents:
      dotnet: "true"
      queue: "katherine-lab"